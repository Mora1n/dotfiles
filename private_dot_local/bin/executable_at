#!/usr/bin/env sh

if tmux list-sessions -F '#{session_name}' >/dev/null 2>&1; then
    exec tmux attach-session
fi

# Let tmux name and attach the initial session when none exists.
exec tmux new-session
