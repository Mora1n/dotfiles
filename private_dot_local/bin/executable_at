#!/usr/bin/env sh

if tmux list-sessions -F '#{session_name}' >/dev/null 2>&1; then
    exec tmux attach-session
fi

# Create the initial session only when no tmux server/session exists.
exec tmux new-session -d -s 0 \; attach-session -t 0
